# nginx-brotli-mysql-phpmyadmin

Автоматический установщик LEMP стека (Linux, Nginx, MariaDB, PHP) с поддержкой Brotli сжатия и phpMyAdmin для Debian 12.

## 🚀 Что устанавливается

- **Nginx** (собран из исходников с модулем Brotli)
- **PHP 8.2** (FPM) с расширениями: mysql, curl, zip, gd, xml, mbstring, imap, bz2, intl, gmp
- **MariaDB** (MySQL) с удаленным доступом
- **phpMyAdmin** (доступ через `/phpmyadmin`)
- **UFW** (firewall) с настройками для Nginx
- **Certbot** (для Let's Encrypt SSL сертификатов)

## 📋 Требования

- Debian 12 (Bookworm)
- Права root
- Минимум 1GB RAM (рекомендуется 2GB+)
- Стабильное интернет-соединение

## ⚡ Быстрая установка

Выполните на сервере с правами root:

```bash
wget --no-cache -qO- https://raw.githubusercontent.com/totalnewera-boop/nginx-brotli-mysql-phpmyadmin/main/lemp-debian.sh | bash
```

Или скачайте и запустите вручную:

```bash
wget https://raw.githubusercontent.com/totalnewera-boop/nginx-brotli-mysql-phpmyadmin/main/lemp-debian.sh
chmod +x lemp-debian.sh
./lemp-debian.sh
```

## 📝 Что делает скрипт

1. **Настраивает SSH keepalive** (предотвращает разрывы соединения)
2. **Устанавливает зависимости** для сборки Nginx
3. **Собирает Nginx из исходников** с модулем Brotli от Google
4. **Настраивает Nginx** с оптимизированными параметрами:
   - Gzip и Brotli сжатие
   - Оптимизированные таймауты
   - Защита от DDoS (rate limiting)
   - Логирование
5. **Устанавливает PHP 8.2-FPM** со всеми необходимыми расширениями
6. **Устанавливает MariaDB** и создает пользователя `admin` с автоматически сгенерированным паролем
7. **Настраивает удаленный доступ** к MariaDB (bind-address = 0.0.0.0)
8. **Устанавливает phpMyAdmin** и настраивает доступ через `/phpmyadmin`
9. **Настраивает UFW firewall** (разрешает SSH, HTTP, HTTPS, MySQL)
10. **Устанавливает Certbot** для получения SSL сертификатов

## 🔐 Доступ после установки

После завершения установки скрипт выведет:

```
====================================
 УСТАНОВКА ЗАВЕРШЕНА
====================================
phpMyAdmin: http://ВАШ_IP/phpmyadmin
DB admin user: admin
DB admin password: СГЕНЕРИРОВАННЫЙ_ПАРОЛЬ
====================================
```

### phpMyAdmin

- **URL**: `http://ВАШ_IP/phpmyadmin`
- **Логин**: `admin`
- **Пароль**: (будет показан в конце установки)

### MariaDB

- **Пользователь**: `admin`
- **Пароль**: (будет показан в конце установки)
- **Удаленный доступ**: включен (порт 3306)

## 🌐 Установка нового сайта

После установки LEMP стека вы можете быстро добавить новый сайт с автоматической настройкой SSL:

### Быстрая установка сайта

Выполните на сервере с правами root (с указанием домена):

```bash
wget --no-cache -qO- https://raw.githubusercontent.com/totalnewera-boop/nginx-brotli-mysql-phpmyadmin/main/install-site.sh | bash -s fitness-journey.bond
```

Или скачайте и запустите вручную:

```bash
wget https://raw.githubusercontent.com/totalnewera-boop/nginx-brotli-mysql-phpmyadmin/main/install-site.sh
chmod +x install-site.sh
./install-site.sh fitness-journey.bond
```

Если домен не указан, скрипт запросит его интерактивно:

```bash
./install-site.sh
Введите домен (например: fitness-journey.bond): fitness-journey.bond
```

Скрипт автоматически:
- Создаст директорию `/var/www/fitness-journey.bond/public`
- Настроит права доступа
- Создаст тестовый `index.php`
- Настроит Nginx виртуальный хост
- Получит SSL сертификат через Let's Encrypt
- Настроит автоматическое обновление сертификата

После установки сайт будет доступен по адресам:
- `https://fitness-journey.bond`
- `https://www.fitness-journey.bond`

## 📁 Структура проекта

- `lemp-debian.sh` - основной скрипт установки LEMP стека
- `install-site.sh` - скрипт для установки нового сайта с SSL
- `setup-vhost.sh` - скрипт для создания виртуальных хостов
- `auto_sync.py` - автоматическая синхронизация с GitHub (для разработки)
- `start_sync.bat` - запуск автосинхронизации на Windows

## 🐛 Решение проблем

### Nginx не запускается

```bash
nginx -t  # Проверка конфигурации
systemctl status nginx
journalctl -xe  # Просмотр логов
```

### PHP-FPM не работает

```bash
systemctl status php8.2-fpm
systemctl restart php8.2-fpm
```

### MariaDB не доступна

```bash
systemctl status mariadb
mysql -u admin -p  # Введите пароль, показанный при установке
```

## 📄 Лицензия

MIT License - см. файл [LICENSE](LICENSE)

## 🤝 Вклад в проект

Любые вопросы или предложения? Откройте [Issue](https://github.com/totalnewera-boop/nginx-brotli-mysql-phpmyadmin/issues) или создайте Pull Request!

---

**Внимание**: Этот скрипт предназначен для быстрой настройки development/staging окружений. Для production используйте дополнительные меры безопасности.
